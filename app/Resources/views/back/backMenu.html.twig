{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path("new_quizz" )}}">Nouveau Quizz</a>
    <a href="{{ path("old_quizz" )}}">Tout les Quizz</a>
    <a href="{{ path("user_datas" )}}">Données utilisateurs</a>
{% endblock %}
